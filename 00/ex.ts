import Letter from "./alphabet.ts";

enum State {
  ELIMINAME
  /**
   * Completar
   */
}
function transition(state: State, letter: Letter): State {
  /**
   * Completar
   */
  return State.ELIMINAME
}

export const machineDescription = {
  transition,
  initial: /**
  * Completar
  */State.ELIMINAME,
  acceptance: [/**
    * Completar
    */],
};
